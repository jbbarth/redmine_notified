$('#ajax-modal').html('<%= j render 'notified/list' %>')
showModal('ajax-modal', '600px')
$('#ajax-modal').addClass('is-notified')

// First init
$(".modal .notification").hide()
$(".modal #notification-issue-<%= @issue_notifications.first.try(:id) %>").show()
